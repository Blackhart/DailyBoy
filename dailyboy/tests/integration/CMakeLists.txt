# dailyboy — integration tests (included only when BUILD_TESTING=ON)
#
# Examples (future):
#   test__makeDaily__valid_job__produces_outputs

# Placeholder — will link libdailyboy and bundled runtime paths.
